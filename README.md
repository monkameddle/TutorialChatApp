Client
nach dem verbinden muss 'reg:meinName' als erste nachricht an den server folgen.

weitere commandos:

command:list -> Liste der nutzer
msg:USername:Message -> um eine Nachricht an einen bestimmten nutzer zu senden
